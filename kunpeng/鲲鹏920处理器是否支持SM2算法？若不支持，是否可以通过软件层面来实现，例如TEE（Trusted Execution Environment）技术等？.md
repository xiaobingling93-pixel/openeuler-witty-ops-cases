# 鲲鹏920处理器是否支持SM2算法？若不支持，是否可以通过软件层面来实现，例如TEE（Trusted Execution Environment）技术等？

## 内核版本


## 问题现象
用户询问鲲鹏920处理器是否支持SM2国密算法，如果不支持，是否可以通过TEE等软件技术实现。

## 问题根因
鲲鹏920处理器没有包含SM2的专用指令或硬件加速引擎，因此不支持SM2算法的硬件加速。

## 解决方案
可以通过软件编程方式实现SM2算法功能，例如使用开源的GmSSL软件库。虽然处理器本身不支持SM2硬件加速，但软件实现是可行的。

