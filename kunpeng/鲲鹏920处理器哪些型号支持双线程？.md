# 鲲鹏920处理器哪些型号支持双线程？

## 内核版本


## 问题现象
用户希望了解鲲鹏920处理器中哪些具体型号支持双线程（即SMT，同步多线程）功能。

## 问题根因
鲲鹏920处理器系列中部分型号支持双线程，而其他型号（如5230、5220、48核型号等）明确不支持。是否支持双线程取决于具体CPU型号的设计。

## 解决方案
可通过在Linux系统中执行lscpu命令，查看输出中的“Thread(s) per core”字段判断是否启用双线程：若值为2，则表示该型号支持并已开启双线程；若为1，则不支持或未开启。具体支持情况需参考对应处理器型号的官方文档或技术规格。

