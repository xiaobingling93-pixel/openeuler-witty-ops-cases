# 编译SU2 7.0.4 时报错

## 内核版本


## 问题现象
编译SU2时报错，报错信息为“configure: error: cannot find python-config for /usr/bin/python.”。

## 问题根因
缺乏依赖python-devel。

## 解决方案
在编译安装之前，执行以下命令安装依赖：yum install python-devel -y。

