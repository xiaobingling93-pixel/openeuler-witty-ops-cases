# 鲲鹏处理器如何降核？

## 内核版本


## 问题现象
用户希望减少鲲鹏处理器的在线CPU核心数，以降低功耗或进行性能调优。

## 问题根因


## 解决方案
可以使用命令 echo 0 > /sys/devices/system/cpu/cpu*X*/online（X代表CPU号）临时关闭指定CPU核心，重启后失效。若需长期生效，可采用以下两种方法：1）编写开机脚本，在系统启动时自动将指定CPU设为offline；2）在grub命令行中添加Kernel参数 maxcpus=n（n为期望启动的最大CPU数量），该参数仅在系统启动时生效。

