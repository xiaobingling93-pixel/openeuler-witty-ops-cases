# 配置操作系统安装依赖包

## 内核版本


## 问题现象
操作系统本地镜像源存在部分安装包缺失的情况，导致无法直接安装某些依赖包。

## 问题根因
本地镜像源不完整，缺少特定依赖包。

## 解决方案
从对应操作系统官方镜像源网站下载缺失的依赖包，上传至服务器后，根据操作系统类型使用 rpm -i（CentOS）或 dpkg -i（Debian/Ubuntu/UOS 20/凝思）命令进行安装。

