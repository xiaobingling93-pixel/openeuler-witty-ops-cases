# 软件迁移评估提示“报告不存在，请重新扫描”

## 内核版本


## 问题现象
在使用鲲鹏DevKit进行软件迁移评估时，系统提示“报告不存在，请重新扫描”。

## 问题根因
上传文件路径权限不足，分析路径的属主权限未正确设置为devkit用户。

## 解决方案
手动赋权分析路径的属主权限，确保属主为devkit。以软件迁移评估为例，分析路径为“/opt/DevKit/workspace/devadmin/porting/package/”，需确认porting/package/两级目录的属主权限正确。

