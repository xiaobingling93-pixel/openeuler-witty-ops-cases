# 升级加速器驱动失败的解决办法

## 内核版本


## 问题现象
升级加速器驱动后，重启系统驱动版本仍为旧版本。

## 问题根因
在升级加速器驱动前，系统更新了其他驱动包（如网卡驱动）或人为更新了initramfs文件系统，导致未升级前的加速器驱动被包含在initramfs中，系统重启时优先从initramfs加载旧驱动。

## 解决方案
升级加速器驱动版本后，执行 dracut --force 命令重新更新initramfs文件系统。

