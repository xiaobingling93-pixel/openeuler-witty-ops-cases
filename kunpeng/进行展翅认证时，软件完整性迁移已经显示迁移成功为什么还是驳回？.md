# 进行展翅认证时，软件完整性迁移已经显示迁移成功为什么还是驳回？

## 内核版本


## 问题现象
在进行鲲鹏展翅认证时，软件完整性迁移已显示迁移成功，但认证申请仍被驳回。

## 问题根因
认证驳回的原因是待验证数替换未为0。即使迁移显示成功，若待验证数替换不为0，则不符合认证通过条件。

## 解决方案
需确保待验证数替换为0；如果确实无法将待验证数替换设为0，则用户必须执行全量测试，并且功能用例数量至少达到30个以上。

