# 鲲鹏920处理器是否支持超线程？例如8核的CPU是否可以超线程到16核？

## 内核版本


## 问题现象
用户询问鲲鹏920处理器是否支持超线程技术，例如8核CPU能否通过超线程变为16核。

## 问题根因
鲲鹏920处理器系列部分型号支持超线程（SMT），但多数常见型号（如5220、5230、48核型号等）明确不支持超线程，一个物理核心仅对应一个物理线程。

## 解决方案
需根据具体处理器型号确认是否支持超线程。可通过lscpu命令查看'Thread(s) per core'字段：若为2则支持超线程，若为1则不支持。对于不支持超线程的型号，无法通过配置开启该功能。

