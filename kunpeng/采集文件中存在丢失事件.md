# 采集文件中存在丢失事件

## 内核版本


## 问题现象
I/O分析任务失败，查看日志发现错误码：“分析数据入库失败，采集文件中存在丢失事件，请尝试增大buffer_size_kb文件中的值”。

## 问题根因
/sys/kernel/tracing/buffer_size_kb中的配置值过小。

## 解决方案
将/sys/kernel/tracing/buffer_size_kb的值加大至二倍数值。例如：echo 2816 > /sys/kernel/tracing/buffer_size_kb

