# 关于KAEZlib块解压接口异常使用场景说明

## 内核版本


## 问题现象
在使用KAEZlib的uncompress块解压接口时，若目标缓冲区容量小于解压后的实际数据量，可能触发未定义行为，导致程序运行结果不可预测。

## 问题根因
uncompress接口要求调用者预先提供足够大的目标缓冲区，但在原始压缩数据大小未知的情况下，用户难以准确预估解压后所需空间，从而容易造成缓冲区不足。

## 解决方案
在解压缩原始数据大小未知的场景下，建议优先采用流式解压模式（inflate）；如果必须使用块解压接口（uncompress），则应分配足够大的目标缓冲区以确保容量充足。

