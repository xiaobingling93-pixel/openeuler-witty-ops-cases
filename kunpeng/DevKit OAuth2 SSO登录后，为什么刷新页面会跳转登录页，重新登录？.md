# DevKit OAuth2 SSO登录后，为什么刷新页面会跳转登录页，重新登录？

## 内核版本


## 问题现象
DevKit OAuth2 SSO登录后，刷新页面会跳转到登录页，需要重新登录。

## 问题根因
DevKit将SSO视为子系统集成，每次刷新页面都会重新获取主系统的授权，以确保双方系统token有效性一致。如果主系统token仍在有效期内，DevKit会快速重新鉴权并自动登录；否则需重新登录。

## 解决方案
确保主系统token在有效期内，刷新页面时DevKit会自动完成重新鉴权并保持登录状态。

