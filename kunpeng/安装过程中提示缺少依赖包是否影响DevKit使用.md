# 安装过程中提示缺少依赖包是否影响DevKit使用

## 内核版本


## 问题现象
在安装鲲鹏DevKit过程中提示缺失某些依赖包，用户不确定是否会影响DevKit的正常使用。

## 问题根因
安装过程中提示的依赖包分为必选依赖包和可选依赖包。必选依赖包缺失会导致安装失败；可选依赖包缺失不会导致安装失败，但可能影响部分功能的完整性。

## 解决方案
若安装过程未失败，则可忽略可选依赖包的缺失，不影响DevKit基本使用。如需使用相关功能，建议配置好repo源或单独编译依赖包并配置环境变量。

