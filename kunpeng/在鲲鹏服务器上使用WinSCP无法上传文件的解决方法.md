# 在鲲鹏服务器上使用WinSCP无法上传文件的解决方法

## 内核版本


## 问题现象
连接UniVPN后，使用WinSCP向鲲鹏服务器上传文件时速度很慢，并在10秒后提示失败。

## 问题根因
可能由于网络不稳定导致上传失败；若传输中断，大概率是电脑休眠导致虚拟网卡停止工作，进而使VPN断开。

## 解决方案
1. 传输大文件建议使用WinSCP以支持断点续传；2. 推荐使用SecoClient客户端替代UniVPN以获得更快的下载速度（6~10M/s）；3. 修改电脑电源设置，防止休眠导致VPN断开。

