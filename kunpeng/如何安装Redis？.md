# 如何安装Redis？

## 内核版本


## 问题现象
Redis安装过程中使用的软件多基于x86环境编译得到，若包含非跨平台语言（如C/C++）实现的模块，直接在鲲鹏服务器上使用会出现不兼容的问题。

## 问题根因
Redis软件包通常在x86环境下编译，其中包含的非跨平台代码在鲲鹏（ARM架构）服务器上无法直接兼容运行。

## 解决方案
需下载Redis源码包，在鲲鹏服务器上重新编译后再部署。具体操作可参考：Redis 移植指南（用于源码编译）和Redis 部署指南（用于部署步骤）。

