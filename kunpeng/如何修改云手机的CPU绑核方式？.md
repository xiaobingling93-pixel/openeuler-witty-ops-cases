# 如何修改云手机的CPU绑核方式？

## 内核版本


## 问题现象
用户需要修改云手机的CPU绑核方式，但不清楚具体配置方法。

## 问题根因


## 解决方案
通过修改“cfct_config”文件中的“CPU_BIND_MODE”变量值来设置绑核方式：0表示指定CPU核绑定，1表示绑定NUMA。若选择模式0，还需修改“MODE0_CPU0”、“MODE0_CPU1”等变量，指定每一路容器分配的具体CPU核心，例如将["2,3", "4,5"……]修改为["2,3,4,5", "6,7,8,9"……]以分配4个CPU核。

