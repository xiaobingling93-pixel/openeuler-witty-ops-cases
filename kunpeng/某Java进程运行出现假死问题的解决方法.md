# 某Java进程运行出现假死问题的解决方法

## 内核版本


## 问题现象
客户业务在x86和鲲鹏上进行混部测试时，鲲鹏节点无规律出现假死，不响应主节点心跳查询，被踢出集群。通过top命令确认Java进程存在且CPU占用率稳定，jstat显示young GC次数不增加，垃圾回收器停止工作，jstack显示大部分业务线程处于Block状态。

## 问题根因
JDK的GC机制存在问题，导致相关线程在GC完成后未能正常结束，而是一直运行SpinPause函数（自旋等待），从而引发进程假死。

## 解决方案
将原JDK替换为毕昇JDK，问题未再出现，Java进程假死问题得以解决。

