# 关于KAEGzip工具对于尾部长度字段处理逻辑的说明

## 内核版本


## 问题现象
篡改Gzip文件尾部的长度字段后，Gzip工具会报错，而KAEGzip工具仍能正常解压，两者行为不一致。

## 问题根因
Gzip在解压过程中会对尾部长度字段进行校验，而KAEGzip所依赖的硬件及驱动未实现对该长度字段的校验功能，因此KAEGzip仅基于完整且符合标准协议的核心数据段进行解压，忽略被篡改的长度字段。

## 解决方案
KAEGzip在该场景下采取了不同的处理逻辑，即只要核心数据段完整且符合标准协议，就正常解压出正确的原始数据，而不校验尾部长度字段。

