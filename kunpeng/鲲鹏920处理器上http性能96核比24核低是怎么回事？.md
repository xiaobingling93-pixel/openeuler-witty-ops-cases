# 鲲鹏920处理器上http性能96核比24核低是怎么回事？

## 内核版本


## 问题现象
在鲲鹏920处理器上，96核配置的HTTP性能低于24核配置。

## 问题根因
NUMA跨核问题导致服务器性能不高。当服务端和测试程序运行在不同NUMA节点时，跨节点访问内存带来额外延迟，从而降低性能。

## 解决方案
使用numactl命令将服务端和客户端绑定到同一个NUMA节点上，例如通过numactl -C 0-15启动服务，确保服务和测试程序运行在同一个CPU（NUMA节点）上，避免跨NUMA访问。

