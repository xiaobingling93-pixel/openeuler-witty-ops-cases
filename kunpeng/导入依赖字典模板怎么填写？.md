# 导入依赖字典模板怎么填写？

## 内核版本


## 问题现象
用户不清楚如何填写鲲鹏DevKit中导入依赖字典的模板。

## 问题根因
依赖字典导入为非必选操作，系统已自带依赖字典，但用户若有自定义依赖需补充，则需正确填写字典模板并确保字典名称与模板名称一致，且URL为可访问地址。

## 解决方案
点击“模板表格”获取字典模板，填写能够获取依赖包的URL，并确保导入的字典名称与模板名称保持一致。

