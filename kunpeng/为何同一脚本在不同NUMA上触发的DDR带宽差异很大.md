# 为何同一脚本在不同NUMA上触发的DDR带宽差异很大

## 内核版本


## 问题现象
同一脚本在不同NUMA节点上运行时，观测到DDR带宽存在显著差异。

## 问题根因
绑核到不同NUMA节点时，由于程序逻辑和当前CPU核及内存使用情况不同，导致缓存未命中（cache miss）程度不同；cache miss增加会引发更多的DDRC流量，从而造成DDR带宽差异显著。

## 解决方案
分析脚本在绑定不同NUMA节点时的执行耗时，定位因cache miss导致的性能瓶颈，并据此进行优化。

