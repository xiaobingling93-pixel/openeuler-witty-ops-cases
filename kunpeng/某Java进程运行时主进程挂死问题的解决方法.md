# 某Java进程运行时主进程挂死问题的解决方法

## 内核版本


## 问题现象
某软件移植到鲲鹏服务器后，压测3小时多可用内存耗尽，服务主进程挂死；而相同条件下x86平台压测8小时无异常。

## 问题根因
鲲鹏平台JVM线程栈默认大小为2048KB，而x86平台为1024KB，在相同最大内存配置下，鲲鹏平台因线程栈更大导致可创建线程数减少、内存占用更高，实际使用内存超过最大内存设定值的95%，可能引发OOM导致进程挂死。

## 解决方案
将鲲鹏平台JVM线程栈大小调整为与x86一致（1024KB），经过长时间稳定性测试后问题不再复现。

