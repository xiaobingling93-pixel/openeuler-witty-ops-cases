# VS Code中在线安装好DevKit后，是否可以离线进行使用？

## 内核版本


## 问题现象
用户在VS Code中在线安装鲲鹏DevKit后，希望了解是否可以在离线环境下继续使用该工具。

## 问题根因
DevKit的VS Code端与Linux服务端之间依赖局域网内的Nginx服务进行通信，若两者处于同一局域网且服务端已正确部署，则可支持离线使用。

## 解决方案
确保VS Code端和Linux服务端位于同一局域网内，即可在离线状态下使用已安装的DevKit功能，因为其依赖的Nginx服务在局域网内仍可访问。

