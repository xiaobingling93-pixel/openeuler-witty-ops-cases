# PATH环境变量修改后在执行环境上未生效的解决方法

## 内核版本


## 问题现象
在服务器环境上修改PATH环境变量后，相关修改在Jenkins流水线脚本执行环境上未生效。

## 问题根因
Jenkins流水线脚本执行时使用的PATH环境变量是在Jenkins进程启动时从服务器环境拷贝而来的，后续对服务器环境变量的修改不会自动同步到已运行的Jenkins进程中。

## 解决方案
在流水线脚本中通过source命令加载配置文件，或直接在脚本中定义所需环境变量；或者重启Jenkins服务以同步最新的服务器环境变量。

