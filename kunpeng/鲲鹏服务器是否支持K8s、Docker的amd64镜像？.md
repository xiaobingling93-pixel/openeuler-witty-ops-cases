# 鲲鹏服务器是否支持K8s、Docker的amd64镜像？

## 内核版本


## 问题现象
用户在鲲鹏服务器上尝试使用K8s或Docker的amd64架构镜像时遇到兼容性问题。

## 问题根因
鲲鹏服务器采用ARM架构（aarch64），而amd64镜像是为x86-64架构编译的，两者指令集不兼容，因此无法直接运行。

## 解决方案
应使用专为aarch64架构构建的镜像。在部署K8s或Docker应用时，确保所用镜像支持ARM架构，或通过多架构镜像（manifest list）自动适配。

