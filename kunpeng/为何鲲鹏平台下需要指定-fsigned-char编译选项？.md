# 为何鲲鹏平台下需要指定-fsigned-char编译选项？

## 内核版本


## 问题现象
在将应用从x86架构迁移到鲲鹏（AArch64）平台时，char类型变量的行为不一致，可能导致程序逻辑错误或运行异常。

## 问题根因
char变量在不同CPU架构下的默认符号性不同：在x86架构下默认为signed char，而在AArch64（鲲鹏）平台下默认为unsigned char。

## 解决方案
在编译选项中加入“-fsigned-char”，强制指定鲲鹏平台下的char类型为有符号数，以保持与x86平台行为一致。

