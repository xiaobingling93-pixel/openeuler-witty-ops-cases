# 节点状态变为离线的解决方法

## 内核版本


## 问题现象
节点从安装成功状态变为离线状态。

## 问题根因
没有同步更新节点的Server和Agent证书。

## 解决方案
1. 重新生成证书：单击Agent服务证书界面里的生成证书按钮，重新生成证书。
2. 确认节点类型：若为本机节点则需执行步骤3及步骤4，若为手动添加节点则只需要执行步骤4。
3. 更换证书：更换离线节点对应的Server证书和Agent证书，分别单击更换证书。
4. 确认节点状态：等待一段时间，节点状态会变为在线状态。

