# 【源码迁移】依赖库跨平台如何处理？

## 内核版本


## 问题现象
在将应用从x86架构迁移到ARM架构（如鲲鹏平台）时，构建过程中使用的依赖库可能存在跨平台兼容性问题，导致迁移失败或运行异常。

## 问题根因
不同架构平台（如x86与ARM）下的依赖库可能存在差异，部分依赖库在目标平台（如鲲鹏）上可能不存在、版本不兼容或需重新编译。

## 解决方案
使用鲲鹏DevKit迁移工具自动识别构建文件中使用的依赖库，工具会分析并判断鲲鹏平台是否存在同名依赖库：若存在，则提供下载源；若不存在，则给出明确提示，指导用户进行替换或适配。

