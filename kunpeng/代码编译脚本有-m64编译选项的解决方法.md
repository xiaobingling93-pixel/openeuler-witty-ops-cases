# 代码编译脚本有-m64编译选项的解决方法

## 内核版本


## 问题现象
在ARM64平台编译时出现告警信息：gcc: error: unrecognized command line option ‘-m64’。

## 问题根因
-m64是x86-64位应用的编译选项，用于为AMD的x86-64架构生成代码，设置int为32位，long和指针为64位。该选项在ARM64平台不被支持。

## 解决方案
将ARM64平台对应的编译选项修改为-mabi=lp64。

