# 鲲鹏920处理器中的48核处理器是否支持双线程？

## 内核版本


## 问题现象
用户询问鲲鹏920处理器中的48核处理器是否支持双线程（即超线程/SMT）。

## 问题根因
鲲鹏920处理器中的48核型号（如5250）硬件设计上不支持SMT（同步多线程）技术，每个物理核心仅对应一个物理线程。

## 解决方案
鲲鹏920处理器中的48核处理器不支持双线程，一个物理核心对应一个物理线程。若需确认具体型号是否支持双线程，可通过lscpu命令查看'Thread(s) per core'字段，值为1表示不支持，值为2表示支持。

