# 鲲鹏920处理器线程数如何查询？

## 内核版本


## 问题现象
用户希望了解如何查询鲲鹏920处理器的线程数。

## 问题根因
鲲鹏920处理器的物理线程数与物理核心数一致，一个物理核心对应一个物理线程，且部分型号不支持超线程（SMT）。

## 解决方案
可通过执行lscpu命令查看系统CPU信息，其中“Thread(s) per core”字段表示每个核心的线程数。若值为1，则表示不支持超线程，线程数等于核心数；若值为2，则表示支持双线程。此外，鲲鹏社区文档也指出，鲲鹏920处理器的核数即为其线程数。

