# 共享目录中创建工作空间失败的解决方法

## 内核版本


## 问题现象
执行分析任务失败，提示信息为：任务采集失败，pidstat命令执行失败；或共享目录中创建工作空间失败。

## 问题根因
采集任务时，sudo命令可能无s权限。

## 解决方案
1. 查看sudo命令是否具有s权限：执行命令 'll /usr/bin/sudo'；2. 若无s权限，则添加s权限：执行命令 'chmod u+s /usr/bin/sudo'。

