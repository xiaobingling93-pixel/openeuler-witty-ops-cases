# ExaGear转码包中哪个文件是用于将Arm32+64位转换为64位的？

## 内核版本


## 问题现象
用户不清楚ExaGear转码包中哪个文件用于将Arm32+64位转换为64位。

## 问题根因


## 解决方案
ExaGear转码包中有三个二进制文件，分别是ubt_a32a64、preubt_a32a64_a64和preubt_a32a64_x64，其中ubt_a32a64文件用于将Arm32+64位转换为64位。详细内容可参考《ExaGear ARM32-ARM64 2.0.0.2 用户指南》。

