# 数据IO应用SDK部署失败的解决方法

## 内核版本


## 问题现象
数据IO应用SDK部署失败，报错信息包括：目标服务器不支持安装SDK；kunpeng-sdk软件包和OS版本不匹配。

## 问题根因
报错一：目标服务器系统不是openEuler 20.03 LTS SP1、openEuler 22.03 LTS SP1或Kylin V10 SP1系统。报错二：上传的SDK软件包和目标系统不匹配。

## 解决方案
报错一：请更换部署SDK的目标服务器。报错二：请上传与目标系统匹配的SDK安装包。

