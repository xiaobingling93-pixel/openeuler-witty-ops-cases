# VS Code安装DevKit工具时是否可以修改默认安装路径？

## 内核版本


## 问题现象
用户希望在VS Code中安装鲲鹏DevKit工具时修改默认安装路径。

## 问题根因
DevKit工具在VS Code中的安装机制限制了路径的自定义：离线安装不支持修改工具默认安装路径，在线安装不支持修改安装包临时存放路径。

## 解决方案
目前不支持修改默认安装路径。若需指定路径，建议采用其他部署方式或联系技术支持确认是否有替代方案。

