# 带宽测试时间较长，测试结果低于预期

## 内核版本


## 问题现象
Ascend DMI工具的带宽测试时间较长，且测试结果明显低于预期。

## 问题根因
当前环境开启了swap交换空间机制，该机制在内存不足时将部分内存数据存储到硬盘上，影响了带宽测试结果的准确性。

## 解决方案
1. 执行free -h检查swap交换空间机制是否已启用；2. 若已启用，在进行带宽测试前以root用户执行swapoff -a关闭swap交换空间。

