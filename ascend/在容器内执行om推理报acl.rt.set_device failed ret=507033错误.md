# 在容器内执行om推理报acl.rt.set_device failed ret=507033错误

## 内核版本


## 问题现象
在容器内执行OM推理时失败，报错信息为：acl.rt.set_device failed ret=507033。

## 问题根因
用户对NPU进行了算力切分，但在启动容器时仍尝试使用整张物理NPU卡，而未指定切分后的虚拟NPU（vNPU）设备，导致设备设置失败。

## 解决方案
应销毁已创建的vNPU，或在启动容器时正确挂载并使用算力切分后生成的vNPU设备，而非整张物理卡。

