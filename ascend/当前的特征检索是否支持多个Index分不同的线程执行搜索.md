# 当前的特征检索是否支持多个Index分不同的线程执行搜索

## 内核版本


## 问题现象
在多线程场景中，若一个昇腾AI处理器上创建了来自Host侧两个线程的两个Index，则Device在处理第一个Index请求的过程中，第二个Index的请求会被阻塞。

## 问题根因
每一个昇腾AI处理器一次只能处理来自同一个进程的一个请求，不支持同时处理Host侧两个不同进程的请求。

## 解决方案
在同一进程的多线程场景下，可让不同线程共享一个Device以实现特征检索；但需注意避免多个线程同时发起对不同Index的请求，以免造成请求阻塞。

