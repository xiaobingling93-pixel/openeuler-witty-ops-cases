# 安装CANN包时提示空间不足

## 内核版本


## 问题现象
CANN软件包安装时所需空间较大，如果安装时遇到空间不足，提示"Not enough space"等信息。

## 问题根因
当前安装目录的可用空间不足。

## 解决方案
提示空间不足时，可以通过TMPDIR环境变量，将解压的临时文件存放目录修改为剩余空间更大的磁盘上的目录，配置后再执行安装操作。示例命令：export TMPDIR=/path/to/directory（需确保路径存在且有足够权限）。

