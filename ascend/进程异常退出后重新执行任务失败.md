# 进程异常退出后重新执行任务失败

## 内核版本


## 问题现象
在DVPP、模型推理或应用/算子开发等业务场景中，当进程因异常退出（如通过ctrl + c或kill命令强行终止）后，立即重新启动任务会导致任务执行失败。

## 问题根因
进程异常退出后，系统需要最长一分钟的时间来检测并释放相关资源。若在此资源释放完成前启动新任务，会因资源未完全释放而导致任务失败。

## 解决方案
在进程异常退出后，需等待至少一分钟，确保系统完成资源释放后再重新执行任务，以保证任务成功运行。

