# 诊断失败，日志提示[Errno 24] Too many open files

## 内核版本


## 问题现象
在使用诊断功能时，若集群规格较大，输入目录下日志文件数较多，导致诊断失败，日志报“Too many open files”错误。

## 问题根因
系统允许同时打开的文件描述符数量不足，无法处理大量日志文件的并发打开操作。

## 解决方案
通过执行 ulimit -n 命令查看当前允许的最大文件描述符数量，并使用 ulimit -n num 命令（如 ulimit -n 2048）临时调高该限制以解决问题。

