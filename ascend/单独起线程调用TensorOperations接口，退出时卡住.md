# 单独起线程调用TensorOperations接口，退出时卡住

## 内核版本


## 问题现象
单独起线程调用TensorOperations的算子接口，接口执行完毕后，部分算子在退出时存在概率性卡死问题。

## 问题根因
单独起线程调用算子接口时，资源的初始化和去初始化未在同一线程中进行，导致退出时卡住。

## 解决方案
通过MxInitFromConfig对相关算子接口进行预加载，具体可参见《Vision SDK 用户指南》的“初始化算子预加载文件实例”章节。

