# 多机拉起DeepSeek-R1模型服务化后，发送推理请求，返回内容乱码

## 内核版本


## 问题现象
四机部署DeepSeek-R1模型并启动服务后，发送推理请求返回的内容为乱码，且无任何错误提示。

## 问题根因
用户使用的模型配置文件与ModelScope、HuggingFace等官方网站提供的原始配置文件存在差异，导致推理结果异常。

## 解决方案
将模型权重目录中的所有配置文件与官网提供的文件进行比对并保持一致，仅修改config.json中的model_type字段为deepseekv2，即可使推理返回正常。

