# 加载大模型时耗时过长

## 内核版本


## 问题现象
加载1300B（十亿参数）大小的模型时耗时过长，约需3个小时。

## 问题根因
未使用异步加载机制，导致模型加载过程串行执行，效率低下。

## 解决方案
通过设置环境变量OMP_NUM_THREADS=1优化OpenMP线程数量，并启用NPU显存碎片收集：export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True 和 export NPU_MEMORY_FRACTION=0.96。优化后加载时间缩短至约10分钟。

