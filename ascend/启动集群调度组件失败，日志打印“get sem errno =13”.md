# 启动集群调度组件失败，日志打印“get sem errno =13”

## 内核版本


## 问题现象
启动集群调度组件失败，组件日志打印“get sem errno =13”。

## 问题根因
KMC密钥文件锁信号量默认权限为600，属主为运行KMC的进程（root或其他用户），普通用户无权限访问root用户创建的信号量，导致无权访问（errno=13）。

## 解决方案
1. 执行命令 'ipcs -s' 查看信号量；2. 执行命令 'ipcrm -S {信号量}' 删除root用户创建的信号量（如0x2016****）；3. 重新部署组件。

