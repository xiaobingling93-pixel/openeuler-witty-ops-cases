# 从开放态部署切换到标准态部署后，在最大库容时，可能出现添加底库卡住的情况

## 内核版本


## 问题现象
从开放态部署切换到标准态部署后，持续添加底库直至超过芯片最大内存时，操作卡住无响应。

## 问题根因
在开放态部署切换至标准态部署过程中，未正确恢复安全启动相关配置，且Device内存被耗尽，导致添加底库操作阻塞。

## 解决方案
建议用户合理规划Device内存使用，避免内存耗尽；切换部署模式时，应先重新安装HDK以恢复安全启动修改，再部署标准态；也可通过设置环境变量${MX_INDEX_SYNCHRONIZE_STREAM_TIME}来指定超时时间，防止长时间卡住。

