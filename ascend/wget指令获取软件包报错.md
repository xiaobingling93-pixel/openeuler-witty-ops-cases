# wget指令获取软件包报错

## 内核版本


## 问题现象
在执行wget指令时，报错：ERROR 403: Forbidden。

## 问题根因
可能的原因包括：1. URL地址拼写错误；2. 当前服务器未连接网络。

## 解决方案
1. 检查并修正URL地址拼写错误后重试；2. 执行'wget www.hiascend.com'验证服务器是否连接网络；3. 若未联网，请确保服务器联网后再执行wget；4. 若服务器无法联网，可在本地PC通过浏览器下载软件包后上传至服务器。

