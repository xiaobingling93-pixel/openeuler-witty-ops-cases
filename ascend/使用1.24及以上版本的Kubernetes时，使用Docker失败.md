# 使用1.24及以上版本的Kubernetes时，使用Docker失败

## 内核版本


## 问题现象
在Kubernetes 1.24及以上版本中，用户无法直接使用Docker作为容器运行时。

## 问题根因
Kubernetes从1.24版本开始移除了对Docker Engine的内置支持组件dockershim，导致Docker无法直接作为容器运行时使用。

## 解决方案
对于已初始化的Kubernetes集群，参考K8s官方文档从dockershim迁移；对于未初始化的集群，安装cri-dockerd并按照其社区指南初始化Kubernetes集群。

