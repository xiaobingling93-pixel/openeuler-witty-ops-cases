# 绑定训练进程到指定CPU

## 内核版本


## 问题现象
在多P训练场景中，Host CPU调度不均匀，影响训练性能。

## 问题根因
未将训练进程绑定到特定的CPU核心，导致Host CPU负载分配不均。

## 解决方案
通过taskset命令将每个训练进程绑定到指定的CPU核心范围。例如，在Total CPU为96、8P训练场景下，每个进程分配12个CPU核心（n = 96 / 8 = 12），Device0绑定0-11，Device7绑定84-95等，从而实现Host CPU调度的均匀分配。

