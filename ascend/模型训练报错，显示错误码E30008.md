# 模型训练报错，显示错误码E30008

## 内核版本


## 问题现象
在使用Rec SDK训练迭代循环下沉功能进行模型训练时，报错显示错误码E30008。

## 问题根因
GetNext算子在完成所有数据训练后，获取不到数据而超时报错。

## 解决方案
方案一：关闭训练迭代循环下沉功能，具体操作参见《CANN 6.3.RC2 TensorFlow 1.15网络模型迁移和训练指南》中“性能调优 > 基本调优 > 训练迭代循环下沉”章节。方案二：提前计算数据集可训练的step数，指定训练step数后退出。

