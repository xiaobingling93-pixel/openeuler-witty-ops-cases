# 片上内存申请失败导致压测失败

## 内核版本


## 问题现象
片上内存压测失败，日志显示：'[ERROR]...[hbm_stress.cpp xxxxx] aclrtMalloc failed, error code: 207001, device:xx' 和 '[ERROR]...[hbm_stress.cpp xxxxx] preOps failed.'

## 问题根因
NPU同时执行其他任务导致片上内存压测时内存申请失败。

## 解决方案
在执行片上内存压测时，确保NPU内存充足，大约需90%以上的NPU空闲内存。

