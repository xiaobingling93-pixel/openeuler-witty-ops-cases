# 驱动安装缺少依赖报错

## 内核版本


## 问题现象
执行命令安装驱动时提示错误：[ERROR]The list of missing tools: lspci,ifconfig。

## 问题根因
系统缺少lspci和ifconfig命令工具，这两个工具分别包含在pciutils和net-tools安装包中。

## 解决方案
根据操作系统类型安装对应依赖包：openEuler系列使用命令 'yum install -y net-tools pciutils'；Debian系列使用命令 'apt-get install -y net-tools pciutils'。

