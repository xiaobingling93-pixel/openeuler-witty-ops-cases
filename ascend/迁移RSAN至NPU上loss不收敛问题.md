# 迁移RSAN至NPU上loss不收敛问题

## 内核版本


## 问题现象
在将RSAN模型从GPU/CPU迁移到NPU（昇腾）平台时，使用PyTorch 1.8.1和CANN 6.0.RC1环境，训练过程中loss不收敛，精度评估结果始终为0。即使添加混合精度训练，问题依然存在。

## 问题根因
在NPU上执行数据切分操作时，与CPU行为存在精度差异，导致模型训练异常。该问题与特定版本中算子的实现精度有关。

## 解决方案
将数据切分操作移至CPU上执行，规避NPU上该算子的精度问题，从而使loss正常收敛，训练结果与GPU基本一致。

