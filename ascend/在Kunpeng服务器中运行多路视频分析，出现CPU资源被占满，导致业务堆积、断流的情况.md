# 在Kunpeng服务器中运行多路视频分析，出现CPU资源被占满，导致业务堆积、断流的情况

## 内核版本


## 问题现象
在Kunpeng 920 7260服务器中运行多路视频分析时，CPU资源被占满，导致业务堆积和断流。

## 问题根因
视频分析业务不要求数据预取，但CPU的预取开关处于打开状态，造成内存带宽浪费，进而导致CPU资源耗尽。

## 解决方案
在BIOS中将CPU的预取开关设置为关闭。

