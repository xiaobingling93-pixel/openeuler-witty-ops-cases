# 发送请求返回乱码

## 内核版本


## 问题现象
使用curl命令、MindIE Client或者Benchmark发起request请求时，Server返回乱码。

## 问题根因
设置代理后，代理服务器无法识别Service配置的内网IP，无法将请求转发给目标服务器，导致请求失败。

## 解决方案
执行以下命令取消代理设置：
unset http_proxy
unset https_proxy

