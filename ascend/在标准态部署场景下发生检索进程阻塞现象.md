# 在标准态部署场景下发生检索进程阻塞现象

## 内核版本


## 问题现象
在标准态部署场景下，执行检索任务时出现以下现象：1）添加底库过程中检索进程阻塞；2）创建并初始化多个Index时检索进程阻塞；3）持续加载Index直至Device内存耗尽后检索进程阻塞。

## 问题根因
在添加底库等操作过程中，Device侧内存耗尽，触发OOM机制，导致相关服务进程被Kill，算子执行状态无法反馈到Host侧。

## 解决方案
用户应合理规划Device内存使用，避免内存耗尽。若需从开放态部署切换为标准态部署，应先卸载开放态部署后再重新部署标准态特征检索。

