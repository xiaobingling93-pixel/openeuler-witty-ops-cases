# Atlas 800-3010 Ubuntu legacy模式下大于2T硬盘自动分区安装无法启动

## 内核版本


## 问题现象
在legacy模式下，使用大于2TB的硬盘自动分区安装Ubuntu 16.04后系统无法启动。

## 问题根因
自动分区方式未正确创建引导所需的分区结构，导致系统启动时找不到引导盘。

## 解决方案
安装Ubuntu时不能使用自动分区，需手动分区。对于大于2TB的硬盘，在legacy模式下至少需要创建四个分区：/boot、biosboot、swap和根分区（/）。其中必须显式创建biosboot分区和/boot分区以确保引导正常。

