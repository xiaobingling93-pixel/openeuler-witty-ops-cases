# 批量安装时提示输入远端服务器密码

## 内核版本


## 问题现象
在使用Ascend Deployer进行批量安装时，尽管已在inventory_file文件中通过ansible_ssh_pass字段配置了远端服务器账号的密码，安装过程中仍然提示需要手动输入远端服务器密码。

## 问题根因
当远端服务器的操作系统为openEuler 20.03LTS、openEuler 22.03LTS或CentOS 7.6时，不支持通过ansible_ssh_pass字段进行密码认证连接。

## 解决方案
应改用SSH密钥认证方式连接远端服务器，具体操作可参考官方文档《远程连接服务器》。

