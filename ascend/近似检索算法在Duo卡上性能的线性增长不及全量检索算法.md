# 近似检索算法在Duo卡上性能的线性增长不及全量检索算法

## 内核版本


## 问题现象
近似检索的IVF系列算法在使用Atlas 300I Duo推理卡时，相较于全量检索算法，其性能的线性增长表现较差。

## 问题根因
IVF系列算法在Atlas 300I Duo推理卡上的线性增长依赖于距离计算在整个search过程中的运算量占比。只有距离计算能均分到多个运算单元，因此在大batch和nprobe较大的场景下线性增长较好，而在小batch和nprobe较小的场景下线性增长较差。

## 解决方案


