# CTyunOS（基于openEuler20.03 自研OS）进行训练时，单卡、8卡训练性能很慢

## 内核版本
openEuler 20.03

## 问题现象
在CTyunOS上进行Bert_Chinese模型训练时，单卡和8卡训练耗时约8小时，而其他操作系统上相同任务仅需约1小时，性能显著下降。

## 问题根因
openEuler系统中默认引入了一个性能优化相关的环境变量，该变量导致Python进程全部绑定到CPU 0核上运行，从而引发训练性能严重劣化。

## 解决方案
unset该环境变量或将其注释掉，即可使训练时长恢复正常。

