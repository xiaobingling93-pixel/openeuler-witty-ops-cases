# NPU卡健康检查返回错误，提示timeout

## 内核版本


## 问题现象
多机拉起DeepSeek模型时，服务化拉起卡住；进行NPU卡健康检查时，返回timeout错误。

## 问题根因
交换机和NPU的网关没有配置，且NPU的网关IP与侦测IP未配置为相同地址。

## 解决方案
使用hccn_tool命令将NPU的网关IP和侦测IP配置为相同的规划地址。具体步骤包括：1) 设置NPU IP和掩码；2) 设置NPU网关；3) 设置NPU侦测地址，确保网关与侦测IP一致。

