# ATC模型转换出现不支持的loop算子报错

## 内核版本


## 问题现象
在使用ATC工具将ONNX模型转换为OM模型的过程中，遇到loop算子不支持的报错。

## 问题根因
当前使用的ATC版本不支持ONNX模型中的loop算子。

## 解决方案
当loop的循环次数固定时，可在导出ONNX模型时使用torch.jit.trace()模式，避免生成loop算子；当循环次数不固定时，需将loop内部逻辑拆分为独立模型，通过多模型串行推理方式处理，循环次数由前一步结果计算或直接传入。

