# 保存Checkpoint文件成功，但找不到该文件

## 内核版本


## 问题现象
保存Checkpoint文件后，对该文件进行操作时，系统提示该文件不存在。

## 问题根因
MindSpore中的load_checkpoint和save_checkpoint接口仅支持以'.ckpt'为后缀的文件名。当传入的文件名不包含'.ckpt'后缀时，系统会自动添加该后缀，导致实际保存的文件名与预期不符。

## 解决方案
在调用保存接口时，手动在传入的文件名末尾加上'.ckpt'后缀，以确保文件名与实际保存的一致。

