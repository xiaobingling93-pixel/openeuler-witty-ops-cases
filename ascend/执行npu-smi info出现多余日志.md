# 执行npu-smi info出现多余日志

## 内核版本


## 问题现象
安装驱动后执行npu-smi info命令，终端输出中出现多余的内核日志信息。

## 问题根因
内核日志级别设置过高，导致内核日志信息被打印到屏幕。

## 解决方案
通过执行命令 'echo 1 4 1 7 > /proc/sys/kernel/printk' 修改内核日志等级，降低控制台日志输出级别，从而避免多余日志显示。

