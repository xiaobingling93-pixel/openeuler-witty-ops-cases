#标题  
SSH服务因主机密钥权限过宽无法启动导致OS无法远程登录  

#内核版本  
2403sp2  

#问题现象  
环境已成功配置OS IP地址并生效，可正常ping通，但无法通过SSH等方式进入操作系统。  

#问题根因  
SSH的主机密钥文件权限太宽松，导致sshd拒绝加载它们。  

#解决方案  
将SSH主机密钥文件的权限修改为600（即仅所有者可读写），即可恢复sshd正常启动和远程登录功能。