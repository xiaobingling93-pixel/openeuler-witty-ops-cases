#标题  
内存满插时 vmcore 无法生成

#内核版本  
6.6.0-28

#问题现象  
在内存满插配置下，即使已设置 swiotlb=32768，系统崩溃后仍无法生成 vmcore 文件。

#问题根因  
1）内核版本 6.6.0-28 存在已知问题（参见 DTS 工单 DTS2025071814925）；  
2）内存不足触发 OOM（Out-Of-Memory），导致 crash dump 机制无法正常工作。

#解决方案  
增大 crashkernel 预留内存，并升级至修复该问题的内核版本，即可正常生成 vmcore。