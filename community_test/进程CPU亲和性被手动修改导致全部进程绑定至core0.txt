#标题
进程CPU亲和性被手动修改导致全部进程绑定至core0

#内核版本
4.19

#问题现象
系统压测期间发现所有进程均运行在CPU core0上，导致其他CPU核心空闲，系统负载不均衡。

#问题根因
有人手动执行了taskset命令修改了进程的CPU亲和性（如：taskset -cp 0-63 3167），将特定进程限制在core0运行，进而影响整体调度行为。

#解决方案
使用taskset命令恢复受影响进程的CPU亲和性至全核范围（例如：taskset -cp 0-63 <PID>），并排查是否有自动化脚本或人为操作错误设置了不当的CPU亲和性策略，避免类似问题再次发生。