#标题  
cgroup从V1切换至V2失败

#内核版本  
2203sp2

#问题现象  
在尝试将cgroup从V1切换至V2时，切换操作未生效；系统启动后仍使用cgroup V1。

#问题根因  
修改grub参数后，执行grub2-mkconfig时指定的路径错误，导致cgroup v2相关内核启动参数未能正确持久化写入GRUB配置文件。

#解决方案  
修正grub2-mkconfig命令的输出路径，确保修改后的GRUB参数（如systemd.unified_cgroup_hierarchy=1）被正确写入并持久化，然后更新引导配置并重启系统。