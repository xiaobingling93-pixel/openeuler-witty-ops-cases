#标题  
在线升级过程中主机core复位

#内核版本  
2203sp4

#问题现象  
在线升级主机113dataturbo至B119版本过程中，主机发生core复位。

#问题根因  
调用alloc_workqueue接口创建odcioq时，传入了一个不支持delayed work的flag，导致系统挂死。

#解决方案  
修正alloc_workqueue调用参数，确保传入的flag与delayed work兼容，避免因不支持的flag导致系统挂死。