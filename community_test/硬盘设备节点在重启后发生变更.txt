#标题
硬盘设备节点在重启后发生变更

#内核版本
24.03 LTS

#问题现象
系统重启后，硬盘设备编号发生变化（例如 /dev/sdb 重启后可能变为 /dev/sdc），导致依赖固定设备路径的配置或脚本失效。

#问题根因
Linux 系统中设备节点命名受硬件探测顺序影响，属于 Linux 内核的正常行为特性，并非操作系统缺陷。

#解决方案
建议使用 UUID 来绑定磁盘设备，或通过配置 udev 规则来固定设备名称，以确保重启后设备路径的一致性。