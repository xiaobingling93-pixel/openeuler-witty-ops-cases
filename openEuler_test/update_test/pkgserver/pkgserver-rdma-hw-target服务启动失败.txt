# 标题
pkgserver “rdma-hw.target服务启动失败”

# 基本信息
- 测试用例: oe_test_service_restart
- 系统版本: openEuler-24.03-LTS-SP3(aarch64/x86_64)
- 是否问题: 否
- 关联issue: 无

# 问题现象
测试执行过程中，检查到服务启动日志中存在报错信息

# 错误日志
```
+ systemctl status rdma-hw.target
+ grep 'Active: active'
+ CHECK_RESULT 1 0 0 'rdma-hw.target start failed'
+ actual_result=1
+ expect_result=0
+ mode=0
+ error_log='rdma-hw.target start failed'
+ exit_mode=0
+ '[' -z '1 0 0 rdma-hw.target start failed' ']'
+ '[' 0 -eq 0 ']'
+ test 1x '!=' 0x
+ test -n 'rdma-hw.target start failed'
```
```
+ systemctl status rdma-hw.target
+ grep 'Active: active'
+ systemctl status rdma-hw.target
+ grep 'inactive (dead)'
     Active: inactive (dead) since Thu 2026-04-02 20:17:07 CST; 38ms ago
+ systemctl status rdma-hw.target
+ grep 'Condition check'
+ grep skip
+ CHECK_RESULT 1 0 0 'rdma-hw.target reload causes the service status to change'
+ actual_result=1
+ expect_result=0
+ mode=0
+ error_log='rdma-hw.target reload causes the service status to change'
+ exit_mode=0
+ '[' -z '1 0 0 rdma-hw.target reload causes the service status to change' ']'
+ '[' 0 -eq 0 ']'
+ test 1x '!=' 0x
+ test -n 'rdma-hw.target reload causes the service status to change'
```
# 问题根因
1. rdma-hw.target依赖物理机RDMA设备

# 解决方案
1. 非问题，设备不支持
